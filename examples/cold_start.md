# Cold-start demo

A topic the system had never seen, taken from nothing to an answered question. Generated by `scripts/m9_cold_start.py`; corpus in `data_demo/`, separate from the eval corpus in `data/`.

**Topic:** `protein structure prediction with deep learning`  
**Date:** 2026-09-16  
**Starting state:** no papers, no chunks, no index

## 1. Indexing

```
$ python main.py --config data_demo/config.yaml index "protein structure prediction with deep learning"
[1/3] searching arXiv for 'protein structure prediction with deep learning' ...
      3 added, 0 already held, tag 'protein_structure_prediction_with_deep_learning'
[2/3] extracting text and figures ...
      91 chunks, 12 figures
[3/3] embedding and indexing ...
      91 embedded, 0 cache hit(s), 91 vectors in the index
```

arXiv query the planner produced: `(abs:("protein structure prediction" OR "protein folding") AND abs:("deep learning" OR "neural network")) AND (cat:q-bio.BM OR cat:cs.LG)`

Papers: 3 · chunks: 91 · figures: 12 · vectors: 91 · wall clock: 929s

## 2. A question the fresh corpus can answer

```
$ python main.py --config data_demo/config.yaml ask "What model architectures do these papers use to predict protein structure, and what do they report about accuracy?"
```

**Tool calls**

```
[0] retrieve_evidence({"query": "protein structure prediction model architecture accuracy", "k": 10}) -> 10 chunk(s), 7028ms
[1] retrieve_evidence({"query": "AlphaFold architecture Evoformer attention layers structure prediction accuracy metrics", "k": 10}) -> 10 chunk(s), 1425ms
[2] retrieve_evidence({"query": "RoseTTAFold architecture three-track network accuracy CASP", "k": 5}) -> 2 chunk(s), 1421ms
[3] check_evidence_consistency({"mode": "groundedness", "chunk_ids": ["2508_18446v1__c0001", "2508_18446v1__c0000", "2508_18446v1__c0003", "2) -> 0 chunk(s), 42871ms
[4] retrieve_evidence({"query": "AlphaFold 3 local mid-scale global module transformer secondary structure domains assemblies", "k":) -> 5 chunk(s), 1373ms
```

**Answer**

Based on the indexed papers, here are the model architectures used for protein structure prediction and their reported accuracy:

## **AlphaFold 2**
Uses advanced neural network architectures that leverage large evolutionary databases and multiple sequence alignments (MSAs). It achieved unprecedented accuracy, effectively solving core aspects of the long-standing protein-folding problem [2508_18446v1__c0001].

## **AlphaFold 3**
Deploys a hierarchical **multi-scale transformer architecture** organized into three parallel modules [2508_18446v1__c0006]:
- **Local-scale transformers**: Focus on short-range interactions among neighboring residues, learning secondary structure elements and local backbone geometry
- **Mid-scale transformers**: Operate at the domain/subdomain level, integrating local features into coherent domain-wise representations and modeling intra-domain contacts and topology
- **Global-scale transformer**: Processes interactions between domains or subunits in complexes, assembling the full tertiary/quaternary structure

Additional architectural innovations include:
- **Biologically-informed cross-attention mechanisms** that integrate evolutionary profiles from MSAs, structural templates, functional motifs, and physico-chemical constraints [2508_18446v1__c0009]
- **Geometry-aware loss functions** optimizing torsion angles, backbone planarity, and stereochemical restraints [2508_18446v1__c0012]

### Reported Accuracy (Figure 3 benchmarks):
| Method | Average GDT_TS | Average TM-score |
|--------|---------------|------------------|
| AlphaFold 3 | **95.6%** | **0.97** |
| AlphaFold 2 | 92.4% | 0.92 |
| RoseTTAFold | 88.1% | 0.88 |

For varying protein lengths (Figure 5), AlphaFold 3 maintains higher accuracy than AlphaFold 2 on longer proteins: short targets (<200 aa) 94.8% vs 93.5%; long targets (>500 aa) 91.2% vs 82.5% [2508_18446v1__c0040].

## **Bi-directional RNN + Artificial Neural Network** (Wang et al., 2019)
A simpler architecture using a bi-directional recurrent neural network combined with an artificial neural network for protein contact map prediction [1906_04527v1__c0000]. Achieved **0.80 accuracy** on a non-redundant PDB database of 8,646 proteins, with accuracy remaining at **0.743** even at sequence distances >128 residues [1906_04527v1__c0001][1906_04527v1__c0005].

*6 iteration(s), 5 tool call(s), 17829 context tokens, stopped: answered, 81s*

## 3. A question it cannot — the abstention case

```
$ python main.py --config data_demo/config.yaml ask "What do these papers say about sourdough fermentation temperature and starter culture biology?"
```

**Tool calls**

```
[0] retrieve_evidence({"query": "sourdough fermentation temperature starter culture biology microbiology", "k": 10}) -> 0 chunk(s), 1368ms
[1] retrieve_evidence({"query": "sourdough temperature fermentation microbial community", "k": 10}) -> 0 chunk(s), 1332ms
[2] analyze_corpus({"operation": "stats"}) -> 0 chunk(s), 3ms
```

**Answer**

The indexed corpus does not contain any papers on sourdough fermentation temperature or starter culture biology. The corpus currently holds 3 papers (2019–2025), all focused on **protein structure prediction with deep learning**.

If you'd like me to search arXiv for recent literature on sourdough fermentation and starter culture microbiology, I can do that — though it will take 30–60 seconds to fetch and index the papers. Would you like me to proceed with a literature search?

*4 iteration(s), 3 tool call(s), 1317 context tokens, stopped: answered, 10s*
